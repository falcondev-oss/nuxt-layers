# @falcondev-oss/nuxt-layers

## Layers

- `base`: lots of helpers, components and plugins

## Usage

```ts
export default defineNuxtConfig({
  extends: ["github:falcondev-oss/nuxt-layers/base#<commit-or-tag-or-branch>"],
})
```
